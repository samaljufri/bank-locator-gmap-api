server:
  port: 8081

spring:
  application:
    name: gmap
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    url: jdbc:h2:mem:locationdb
    driverClassName: org.h2.Driver
    username: SA
    password:
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true